<header><!--heder section or navigation bar section-->
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Shopping Cart-USER </a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">products</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart">Cart
              <span
                class="badge bg-success"
              >{{cartCount}}</span><!--We printing the cart count in here. this cart count which is sent from the user.js file-->
            </a>
          </li>
          <!--User data which is passing from the user.js file / rooter-->
          <!--This is used to check that user is login or not. if the user is login then only it will display the order page-->
          {{#if user}}
            <li class="nav-item">
              <a class="nav-link" href="/orders">Order</a>
            </li>
          {{/if}}
        </ul>
        <div class="dropdown">
          <button
            class="btn btn-secondary dropdown-toggle"
            type="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <!--This is used to print the name of the login user to the navigation bar
            The user information which is sent from the user.js file-->
            {{#if user}}
              {{user.name}}
            {{else}}
              Account
            {{/if}}
          </button>
          <ul class="dropdown-menu">
            {{#if user}}
              <!--This is used to logout the user -->
              <li><a class="dropdown-item" href="/logout">logout</a></li>
            {{else}}
              <li><a class="dropdown-item" href="/login">Login</a></li>
            {{/if}}
            <li><a class="dropdown-item" href="/profile">Profile</a></li>
          </ul>
        </div>
      </div>
    </div>
  </nav>
</header>